# Coral Island Wiki (ภาษาไทย)

## บริบทโปรเจกต์

เว็บ wiki เกม Coral Island ภาษาไทย ทำคนเดียว ไม่มีทีม ไม่มีคนมาช่วยแก้เนื้อหา

เป้าหมายหลัก: **ให้คนไทยที่เล่นเกมนี้เสิร์ช Google แล้วเจอเว็บนี้**
ทุกการตัดสินใจเรื่องโครงสร้างและเนื้อหาให้ชั่งน้ำหนักจากเป้าหมายนี้เป็นหลัก

ตลาดภาษาไทยยังไม่มีคู่แข่ง (Fandom ครองเฉพาะภาษาอังกฤษ) จุดแข็งของเราคือเนื้อหาภาษาไทยที่ใช้คำเดียวกับที่ผู้เล่นเห็นบนจอ

## ภาษาที่ใช้

- คุยกับผู้ใช้เป็นภาษาไทย
- โค้ด ชื่อตัวแปร commit message เป็นภาษาอังกฤษ
- เนื้อหาบนเว็บเป็นภาษาไทย แต่ต้องมีชื่ออังกฤษกำกับเสมอ (ดูกฎด้านล่าง)

## Stack

- Astro + TypeScript (strict mode)
- Tailwind CSS
- Static output ล้วน — ไม่มี SSR ไม่มี database ไม่มี API route
- Deploy: Cloudflare Pages
- Node 22.12+ / npm (Astro 7 ต้องการ)

ข้อมูลทั้งหมดเก็บเป็นไฟล์ JSON และ Markdown ใน repo ไม่ต้องเสนอให้ใช้ database
ถ้าคิดว่าจำเป็นต้องมี ให้หยุดถามก่อนพร้อมเหตุผล

## โครงสร้างโฟลเดอร์

```
├── CLAUDE.md
├── PLAN.md              # ลำดับงาน ทำตามนี้
├── data/
│   ├── raw/             # export ดิบจาก FModel — READ ONLY
│   └── processed/       # ผลลัพธ์จาก scripts/ เท่านั้น
├── scripts/             # สคริปต์แปลงข้อมูล (Node + TypeScript)
└── src/
    ├── content/         # Markdown หน้าไกด์
    ├── components/
    ├── layouts/
    └── pages/
```

## กฎเรื่องข้อมูล

1. `data/raw/` คือไฟล์ที่ export มาจากตัวเกมด้วย FModel **ห้ามแก้ไข ห้ามลบ ห้ามจัดรูปแบบใหม่** ไม่ว่าด้วยมือหรือด้วยสคริปต์ อ่านอย่างเดียว
2. `data/processed/` ต้องเกิดจากการรันสคริปต์ใน `scripts/` เท่านั้น ห้ามแก้ด้วยมือ ถ้าค่าผิดให้ไปแก้ที่สคริปต์
3. **ก่อนเขียนสคริปต์แปลงข้อมูลใด ๆ ต้องเปิดอ่านไฟล์จริงใน `data/raw/` ก่อนเสมอ** ห้ามเดาโครงสร้าง JSON ห้ามเขียนโค้ดโดยอ้างจากรูปแบบทั่วไปของ Unreal Engine
4. ชื่อไอเทม ปลา พืช ตัวละคร **ต้องใช้ชื่อภาษาไทยที่มาจากไฟล์ localization ของเกม ห้ามแปลเอง** ถ้าหาคำแปลไม่เจอ ให้ใส่ค่าเป็น `null` แล้วรายงานรายการที่ขาดออกมา อย่าเติมคำแปลที่คิดขึ้นเอง
5. ข้อมูลที่ไม่มีในไฟล์เกม (เช่นเคล็ดลับการเล่น) ห้ามแต่งขึ้นมา ต้องมาจากผู้ใช้เท่านั้น

## กฎ SEO

- ทุกหน้าต้องมี `<title>` และ meta description ภาษาไทย ไม่ซ้ำกับหน้าอื่น
- slug ใน URL เป็น ASCII ภาษาอังกฤษ (`/fish/angelfish/`) แต่หัวข้อบนหน้าเป็นภาษาไทย
- ทุกหน้าไอเทม/ตัวละคร ต้องแสดงชื่อไทยและชื่ออังกฤษคู่กันใน h1 หรือใต้ h1 ทันที เพราะคนไทยเสิร์ชปนกันทั้งสองแบบ
- **ห้ามสร้างหน้าที่มีเนื้อหาน้อยกว่าประมาณ 150 คำ** ถ้าไอเทมกลุ่มไหนข้อมูลน้อยเกินไป ให้รวมเป็นหน้าตารางเดียวแทนการแตกเป็นหน้าย่อย การปั๊มหน้าบาง ๆ จำนวนมากทำให้ Google ไม่ index
- ต้องมี sitemap.xml และ robots.txt ที่อัปเดตอัตโนมัติตอน build
- ฟอนต์ไทยต้อง preload และกำหนด `font-display: swap` ไม่ให้เกิด layout shift
- ทุกหน้าต้องลิงก์ไปหน้าอื่นที่เกี่ยวข้องอย่างน้อย 3 ลิงก์

## กฎการทำงาน

- ทำงานตามลำดับใน `PLAN.md` **ทำเสร็จทีละ task แล้วหยุดให้ตรวจก่อน** ห้ามไหลไป task ถัดไปเอง
- ก่อนติดตั้ง dependency ใหม่ ให้ถามก่อนพร้อมเหตุผลว่าทำไมของที่มีอยู่ไม่พอ
- commit ย่อย ๆ ต่อหนึ่งงาน ข้อความสั้นและชัด
- ห้าม commit ไฟล์เกิน 5 MB
- ถ้าเจอข้อมูลขัดแย้ง ไม่ครบ หรือโครงสร้างไม่ตรงที่คาด **ให้หยุดแล้วถาม ห้ามเดาค่าขึ้นมาเติม**
- ถ้าเห็นว่าแนวทางที่สั่งมามีปัญหา ให้บอกตรง ๆ ก่อนลงมือ

## ข้อห้าม

- ห้ามคัดลอกหรือแปลเนื้อหาจาก Coral Island Wiki บน Fandom หรือเว็บอื่น (ติดเงื่อนไขลิขสิทธิ์ CC-BY-SA และเป็นงานซ้ำซ้อน)
- ห้ามใส่ข้อมูลตัวเลขในเกมที่ไม่ได้มาจาก `data/` หรือจากผู้ใช้โดยตรง
- footer ทุกหน้าต้องมีข้อความระบุว่าเป็นเว็บแฟนเมด ไม่มีส่วนเกี่ยวข้องกับ Stairway Games

## คำสั่งที่ใช้บ่อย

```bash
npm run dev       # dev server
npm run build     # build เป็น static
npm run preview   # ดูผล build
```

## หมายเหตุ dev server

รัน dev server แบบ background: `npx astro dev --background`
จัดการด้วย `astro dev stop` / `astro dev status` / `astro dev logs`

เอกสาร Astro: https://docs.astro.build
