# แผนงาน Coral Island Wiki ไทย

หลักการ: **เอาเว็บขึ้นจริงให้เร็วที่สุด แล้วค่อยเติมเนื้อหา**
กับดักของโปรเจกต์ส่วนตัวคือใช้เวลาหลายสัปดาห์เตรียมข้อมูลแล้วหมดไฟก่อนเว็บได้ขึ้น
แผนนี้จึงเอาหน้าแรกขึ้นออนไลน์ตั้งแต่ Phase 0 ก่อนจะแตะไฟล์เกมเลย

ทำทีละ task หยุดให้ตรวจทุกครั้ง

---

## Phase 0 — ตั้งโครงและเอาขึ้นออนไลน์

เป้าหมาย: มี URL จริงที่เปิดได้ ภายในวันเดียว

- [ ] **0.1** สร้างโปรเจกต์ Astro + TypeScript strict + Tailwind
- [ ] **0.2** ตั้งค่าฟอนต์ไทย (Noto Sans Thai หรือ IBM Plex Sans Thai) แบบ self-host พร้อม preload
  - เกณฑ์ผ่าน: เปิดหน้าแล้วตัวอักษรไทยไม่กระพริบเปลี่ยนฟอนต์
- [ ] **0.3** สร้าง BaseLayout: header, nav, footer พร้อม disclaimer แฟนเมด
  - ต้องรับ props: `title`, `description`, `ogImage` (optional)
- [ ] **0.4** หน้าแรกชั่วคราว อธิบายว่าเว็บนี้คืออะไร + ลิงก์ไปหน้าไกด์
- [ ] **0.5** ติดตั้ง `@astrojs/sitemap` ตั้ง `site` ใน config ให้ถูก
- [ ] **0.6** deploy ขึ้น Cloudflare Pages
  - เกณฑ์ผ่าน: เปิด URL จริงได้ และ `/sitemap-index.xml` ใช้งานได้

**หยุดตรงนี้** ยังไม่ต้องทำ Phase 1 จนกว่าเว็บจะขึ้นจริง

---

## Phase 1 — เนื้อหาไกด์ (ส่วนที่ไม่ต้องใช้ไฟล์เกม)

เป้าหมาย: 5-8 หน้าไกด์ที่เขียนจากประสบการณ์เล่นเอง นี่คือส่วนที่ Fandom ภาษาอังกฤษให้คนไทยไม่ได้

- [ ] **1.1** ตั้ง content collection `guides` พร้อม schema (zod)
  - fields: `title`, `description`, `publishedAt`, `updatedAt`, `tags[]`, `draft`
- [ ] **1.2** หน้ารวมไกด์ `/guide/` เรียงตาม `updatedAt`
- [ ] **1.3** หน้าไกด์เดี่ยว `/guide/[slug]/`
  - มีสารบัญอัตโนมัติจาก heading (หน้าไกด์ยาว ๆ ต้องมี)
  - แสดงวันที่อัปเดตล่าสุด
- [ ] **1.4** ใส่ JSON-LD `Article` ในหน้าไกด์ และ `BreadcrumbList` ทุกหน้า
- [ ] **1.5** ผู้ใช้เขียนเนื้อหาไกด์เอง 5 หน้าแรก (Claude Code ช่วยจัดรูปแบบ/ตรวจ ไม่ต้องแต่งเนื้อหา)
  - หัวข้อที่คนค้นบ่อย: เริ่มเล่นยังไง / ทำเงินปีแรก / จัดผังฟาร์ม / ดำน้ำกับฟื้นฟูปะการัง / จีบตัวละคร
- [ ] **1.6** ตรวจ Lighthouse บนมือถือ
  - เกณฑ์ผ่าน: Performance และ SEO ไม่ต่ำกว่า 95

---

## Phase 2 — ข้อมูลจากไฟล์เกม เริ่มที่ "ปลา" อย่างเดียว

ทำหมวดเดียวให้จบตั้งแต่ไฟล์ดิบจนขึ้นเว็บก่อน จะเจอปัญหาจริงครบโดยไม่เสียเวลาทั้งก้อน

- [ ] **2.1** ผู้ใช้วางไฟล์ export จาก FModel ลง `data/raw/`
- [ ] **2.2** อ่านไฟล์จริงแล้ว**รายงานโครงสร้างกลับมาก่อน** ยังไม่ต้องเขียนโค้ด
  - ตาราง fish มี field อะไรบ้าง ชนิดข้อมูลอะไร
  - ไฟล์ localization ไทยใช้ key อะไรเชื่อมกับตาราง
  - มี field ไหนที่ยังไม่เข้าใจความหมาย ให้ list ออกมาถาม
- [ ] **2.3** เขียน `scripts/transform-fish.ts` แปลง raw → `data/processed/fish.json`
  - validate ด้วย zod ถ้าข้อมูลไม่ตรง schema ให้ fail พร้อมบอกว่าแถวไหน
  - รายงานจำนวนรายการที่แปลงได้ และรายการที่ไม่มีชื่อไทย
- [ ] **2.4** หน้าตารางรวม `/fish/` — กรองตามฤดูและช่วงเวลาได้ (client-side ไม่ต้องใช้ JS framework)
- [ ] **2.5** หน้าปลาเดี่ยว `/fish/[slug]/` — เฉพาะตัวที่มีข้อมูลพอถึงเกณฑ์ 150 คำ
  - ตัวที่ข้อมูลน้อยให้อยู่แค่ในหน้าตารางรวม
- [ ] **2.6** deploy แล้วเช็กว่า Google index หน้าใหม่ได้ (Search Console)

---

## Phase 3 — ขยายหมวดอื่น

ทำซ้ำรูปแบบเดิมจาก Phase 2 ทีละหมวด เรียงตามที่คนค้นบ่อย

- [ ] **3.1** ของขวัญที่ชาวเกาะแต่ละคนชอบ (หมวดที่คนค้นเยอะที่สุด ทำก่อน)
- [ ] **3.2** พืชผล แยกตามฤดู
- [ ] **3.3** ตัวละคร/ชาวเกาะ พร้อมวันเกิด
- [ ] **3.4** ปฏิทินเทศกาลและวันเกิดรวม
- [ ] **3.5** แมลงและของในพิพิธภัณฑ์
- [ ] **3.6** สูตรอาหารและการคราฟต์

---

## Phase 4 — ขัดให้คม

- [ ] **4.1** ค้นหาในเว็บแบบ static (Pagefind)
- [ ] **4.2** ลิงก์ข้ามหน้าอัตโนมัติ เช่น หน้าปลาโยงไปสูตรอาหารที่ใช้ปลานั้น และโยงไปคนที่ชอบปลานั้นเป็นของขวัญ
- [ ] **4.3** OG image สำหรับแชร์ลงโซเชียล
- [ ] **4.4** สคริปต์เทียบข้อมูลเวอร์ชันเก่า/ใหม่ เวลาเกมออกแพตช์ จะได้รู้ว่าต้องแก้หน้าไหน

---

## หมายเหตุสำหรับ Claude Code

- Phase 0 และ 1 ทำได้เลยโดยไม่ต้องรอไฟล์จากเกม
- Phase 2 เริ่มได้ก็ต่อเมื่อมีไฟล์ใน `data/raw/` แล้วเท่านั้น ถ้ายังไม่มีให้บอกผู้ใช้ อย่าสร้างข้อมูลตัวอย่างขึ้นมาเอง
- ถ้าผู้ใช้สั่งข้ามลำดับ ให้ทำตามที่สั่ง แต่เตือนสั้น ๆ ว่าข้ามอะไรไป
